apiVersion: v1
kind: Service
metadata:
  name: kiebex-service
  namespace: kiebex
  labels:
    app: kiebex
spec:
  selector:
    app: kiebex
  ports:
    - protocol: TCP
      name: web
      port: 80
      targetPort: 3000
